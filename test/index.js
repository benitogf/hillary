'use strict'

const chai = require('chai')
const expect = chai.expect
const hillary = 1

describe('hillary', function () {
  it('should', function () {
    expect(hillary).not.to.be.an.instanceOf(Object)
  })
})
